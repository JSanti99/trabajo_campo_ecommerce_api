{
  "routes": [
    {
      "method": "GET",
      "path": "/facturas",
      "handler": "facturas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/facturas/count",
      "handler": "facturas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/facturas/:id",
      "handler": "facturas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/facturas",
      "handler": "facturas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/facturas/:id",
      "handler": "facturas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/facturas/:id",
      "handler": "facturas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
